[project]
name = "mindbridge"
version = "0.1.0"
description = "Public-safe scaffold for an EEG-to-command BCI interaction pipeline."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

